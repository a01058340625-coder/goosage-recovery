spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://localhost:3306/goosage_local?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: goosage
    password: 1234

server:
  port: 8084
