spring:
  config:
    activate:
      on-profile: edge

  datasource:
    url: jdbc:mysql://EDGE_HOST:3306/goosage_edge
    username: edge_user
    password: edge_pw

server:
  port: 8083

logging:
  file:
    name: logs/edge.log
