spring:
  config:
    activate:
      on-profile: edge

  datasource:
    url: jdbc:mysql://<EDGE_HOST>:3306/goosage_edge?serverTimezone=Asia/Seoul
    username: edge_user
    password: edge_pw

server:
  port: 8083

logging:
  level:
    root: INFO
    org.springframework.web: INFO
